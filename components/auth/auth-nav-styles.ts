/** Outline pill — header “Log in”, “Log out”, and auth-page cross-links. */
export const authNavOutlineClass =
  "inline-flex items-center justify-center rounded-xl border border-card-border px-4 py-2.5 text-sm font-medium text-foreground transition-colors hover:bg-card-hover";

/** Primary pill — header “Sign up” and auth-page cross-links. */
export const authNavPrimaryClass =
  "inline-flex items-center justify-center rounded-xl bg-brand px-4 py-2.5 text-sm font-semibold text-white transition-colors hover:bg-brand-hover";
